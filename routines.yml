# Builders
compile:
    - npx
    - tsc
image:
    - docker
    - build
    - .
    - -t
    - joellefkowitz/autobots:0.2.1

# Runners
run:
    - node
    - dist/main.js
    - keyfile.json
    - --whitelist
    - whitelist.json

new:
    - node
    - dist/main.js
    - keyfile.json
    - --new
    - --whitelist
    - whitelist.json

# Linters
cspell:
    - cspell
    - ./**/*
    - "-c"
    - .cspell.json
eslint:
    - npx
    - eslint
    - "-c"
    - .eslintrc.yml
    - "src/**/*.{js,ts,html,css}"
    - --no-error-on-unmatched-pattern

# Formatters
prettier:
    - prettier
    - "src/**/*.{js,ts,html,css}"
    - --write

# tests
jasmine:
    - npx
    - istanbul
    - cover
    - --include-all-sources
    - jasmine-node
    - dist
