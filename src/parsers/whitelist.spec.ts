import {parseWhitelist} from "./whitelist";

xdescribe("Test parseWhitelist", () => {
  const validWhitelist = "";
  const invalidWhitelist = "";

  it("validWhitelist -> parsed", () => {
    expect().toBe();
  });

  it("invalidWhitelist -> throws", () => {
    expect().toBe();
  });
});
